{
  "name": "javascript-canon",
  "version": "1.0.0",
  "description": "a presentation of the most important learning JavaScript resources",
  "main": "app/app.js",
  "scripts": {
    "server": "nodemon -e css,ejs,js,json --watch app",
    "build-jade": "jade jade/index.jade -o build/",
    "build-js": "browserify js-build/*.js | uglifyjs -mc > build/js/scripts.js",
    "build-sass": "node-sass css-build/style.scss css-build/style.css",
    "concat-css": "cat css-build/*.css | cleancss --s0 -o build/css/style.css",
    "uncss": "grunt",
    "css-build": "npm run build-sass && npm run concat-css && npm run uncss",
    "build": "npm run build-jade && npm run css-build && npm run build-js",
    "watch-jade": "nodemon -e jade -w jade -x 'npm run build-jade'",
    "watch-js": "nodemon -e js -w js-build -x 'npm run build-js'",
    "watch-css": "nodemon -e scss -w css-build -w css-build/imports -x 'npm run css-build'",
    "start": "npm run server & npm run watch-jade & npm run watch-js & npm run watch-css",
    "image-minify": "imagemin image-min/* --out-dir=build/img/",
    "book-minify": "imagemin image-min/book-images/* --out-dir=build/img/book-images/",
    "copy-normalize": "ncp node_modules/normalize.css/normalize.css css-build/normalize.css"
  },
  "author": "kaidez",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.1",
    "clean-css": "^3.4.20",
    "grunt": "^1.0.1",
    "grunt-cli": "^1.2.0",
    "grunt-uncss": "^0.6.1",
    "jade": "^1.11.0",
    "jquery": "^3.1.1",
    "ncp": "^2.0.0",
    "node-sass": "^3.12.2",
    "nodemon": "1.11.0",
    "uglifyjs": "^2.4.10",
    "uncss": "0.14.1"
  },
  "dependencies": {
    "ejs": "^2.5.2",
    "express": "^4.14.0",
    "jquery": "^3.1.0",
    "normalize.css": "^5.0.0",
    "reload": "^1.1.0",
    "underscore": "^1.8.3"
  }
}
